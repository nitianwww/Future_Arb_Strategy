# 项目模块总览

TqSdk 期货跨期套利系统，四大块。每块有专属 skill（`.claude/skills/`），改对应模块前先看对应 skill。

| 块 | 内容 | 主要文件 | Skill |
|---|---|---|---|
| 1 | 交易所规则整理、手续费下载 | all_product_config20260629.xlsx, futures_comm_info.py | `all-product-trading-rules` |
| 2 | 实时行情 + 历史行情处理 | spread_monitor.py, hist_stats.py, hist_tick_daily.py, product_detail.py(仓单) | `market-data-pipeline` |
| 3 | 交易机会筛选 | screener.py, screen_report.py, product_detail.py, roll_ratio_report.py / roll_ratio_recompute.py | `opportunity-screening` |
| 4 | 下单执行算法（仅起步） | spread_trader.py, grid_strategy.py(暂停) | `order-execution` |

## 第 1 块：交易所规则与手续费

- **all_product_config20260629.xlsx**：88 品种主配置表——乘数、tick、仓储费（最高标准）、注销月、可转抛(roll_over)、时间贴水(time_decay JSON)、备注（[仓储费][转抛][注销月][时间贴水][规则断层] 标签）。逐字段对过 TqSdk 实时 + 国泰君安规则表 + 各交易所官网。
- **futures_comm_info.py**：逐合约手续费/保证金（9qihuo 抓取 + TqSdk 合并）。
- 规则细节、核对方法、数据源全部在 skill `all-product-trading-rules`（7 个 references：config-schema / rollover-and-cancel-month / storage-fee / data-sources-and-verification / exchange-facts / spread-monitor-and-ratios / futures-comm-info）。

## 第 2 块：行情处理

- **实时**：spread_monitor.py——交易所组合合约发现（DCE.SP/CZCE.SPD/GFEX.SP）、三腿盘口、合并买卖价、价差比例/仓储费比例/转抛比例，方向恒为近月−远月。
- **历史**：daily_k parquet 缓存（4700+ 合约，date/close/oi，过期合约不可变）、历史同期年份版本统计、期限结构斜率（OI 加权回归）、每日 tick 落盘（I:\futures_data）、仓单（RQData）、快照增量归档（data/snapshots/，只追加）。
- 铁律：F 结算合约全局剔除；wait_update 带 deadline；批量 K线每 250 合约换新连接。

## 第 3 块：机会筛选

- 14 策略（A 类实时 9 + C 类统计 7），近月回归 stub。三段输出：主列表 / 钓鱼桶(OI<5000) / 仅提示(股指国债集运)。
- 全流程 `python screen_report.py`：建 universe → 预热缓存 → 跑策略 → CSV + 快照 → 品种详情图（7 面板）→ 历史同期页 → opportunities.html。
- 页面功能：筛选栏、可转抛标记、选中机会（localStorage 快照式，刷新不变）、自定义新增。
- 转抛比例表：roll_ratio_report.py（CSV 为数据层，recompute 免行情重算）。

## 第 4 块：下单执行（起步阶段）

- spread_trader.py：组合合约被动到价单状态机，dry-run 已验证，**实盘未测**（config.ini [trade] 账户待填）。
- 组合合约仅实盘可交易；单笔委托交易所撮合双腿，无腿风险。
- grid_strategy.py 网格骨架已建，用户暂停中。
- 未做：持仓跟踪、平仓、部分成交补挂、筛选→下单自动触发。

## 公共配置

- **config.ini**：[auth] 天勤 15618075501；[settings] 交易所/max_months/funding_rate/配置表路径；[trade] 实盘账户（空）；[data] root=I:\futures_data（必须在 BaiduSyncdisk 外）。
- 环境：Python 3.13.8（C:\veighna_studio），tqsdk 3.8.6 专业版，RQData(rqdatac) 账号同天勤。
- 输出目录：output/（HTML/CSV 报告）、data/（缓存与归档）。
