# 交易机会筛选逻辑（供进一步过滤参考）

> 代码：`screener.py`（策略与数据模型）+ `screen_report.py`（流程与页面）。方向口径一律 **近月 − 远月**；价差盘口一律用 **组合合约优先，否则两腿 bid/ask 合成**；`mid=(bid+ask)/2`。

---

## 一、总流程

```
build_universe(all_pairs=True)         # 发现合约→订阅盘口→构造 Contract/Spread/Butterfly
  → set_hist/warrant/slope_provider    # 注入历史日K、仓单、期限斜率数据源
  → warm_cache                          # 预热 C 类统计所需历史缓存(分块新连接)
  → run_screen(A_STRATEGIES+C_STRATEGIES)   # 跑 14 策略
  → _annotate                           # 全局标注: 流动性/规则断层/仅提示/可转抛
  → 按 score 降序
  → emit CSV + archive_snapshot + 详情图 + 历史同期页
  → write_report → opportunities.html   # 三段表
```

## 二、数据模型（每条机会携带什么，可作过滤字段）

- **Contract**：symbol/product/month/tick/mult/bid/ask/last/**oi**/**days_to_expiry**/is_main/**is_cancel_month**/roll_over
- **Spread**（近−远）：bid/ask/last/mid、source(combo/synth)、abs_price(=近月最新价)、days_to_expiry、involves_main、**cancel_state**(both/near/far/none)、**fullcarry**(满仓持有成本含时间贴水)、decay、months、adjacent
- **Butterfly**（后段−前段）：value、value_norm(=value/绝对价)
- **Opportunity**（输出）：strategy/kind/product/structure/direction/**score**/conditions(各条件实际值,dict)/tradeable(组合合约)/hold_period/**liquid**/**flag**/**rollable**

> `conditions` 里每个策略都塞了触发时的实际数值（分位、夏普、斜率、仓单、OI 等），这些是**你做二次过滤最直接的抓手**——CSV `conditions` 列里全都在。

## 三、三段分类（页面已有的第一层过滤）

| 段 | 条件 | 含义 |
|---|---|---|
| ✅ 主列表 | `liquid and not flag` | 流动性达标、可执行 |
| 🎣 钓鱼桶 | `not liquid` | 任一腿 OI < **5000**（`LIQ_MIN_OI`），只挂单钓鱼 |
| ⚠️ 仅提示 | `flag and liquid` | 规则断层 / 现金交割指数类 |

页面筛选栏（localStorage 记忆）：策略下拉、品种首字母、仅交易所组合、可转抛、**期限结构极端过滤（默认开，阈值可输入默认90）**——对所有 spread 类行按方向判危险：**正套(做多/买近卖远)遇斜率分位>90（深度 contango）隐藏；反套(做空)遇分位<10（深度 back）隐藏**。逻辑：结构处历史极端时，顺结构方向的统计单是在接结构性飞刀。

## 四、14 个策略的触发条件 / 评分 / 方向

### A 类（纯实时，9 个）

| 策略 | 触发条件 | score（强度） | 方向 |
|---|---|---|---|
| **折角修复** | \|蝶式/绝对价\|>**0.003** 且 前后段符号相反 且 不涉及主力 且 同注销状态可比 | \|蝶式/绝对价\| | 上凸→做空蝶式，反之做多 |
| **主力合约折角修复** | 同上 + 中间月=主力 且 主力距到期≤**30**天 | \|蝶式/绝对价\| | 同上 |
| **注销月折价** | 中间月注销&前后非注销→前段<0且后段>0；或中间非注销&前后注销→前段>0且后段<0 | \|前段\|+\|后段\| | 做多/做空前段+反向后段 |
| **价差递延** | \|蝶式/绝对价\|>**0.005** 且 同注销状态可比（不要求符号相反） | \|蝶式/绝对价\| | 上凸→卖后段买前段 |
| **注销后首月溢价** | 前月注销&首月非注销 且 (后月−后后月)<0 且 期限曲线非显著Contango(斜率t≥−2) | \|价差\|/绝对价 | 做多价差(买首月卖次月) |
| **远期高低估** | 同注销状态组内，每月carry率(=mid/月数/绝对价)截面 \|z\|>**2.0** 且 **前腿到期>180天**(远期定义:前腿到期6个月以上) 且 组样本≥4 | \|z\| | z>0做空(高估)，z<0做多(低估) |
| **近月转抛** | 价差/fullcarry>**1.0** 且 距到期>**10**天 且 可转抛(区间无注销月)且近月未进交割月 | 转抛比例 | 买近卖远持有转抛 |
| **远月转抛** | 某远月对(**前腿到期>180天**=远月对定义) 比例>**1.0** 且 所有近月对(前腿≤180天)都<1.0 | 转抛比例 | 买近卖远(远月对) |
| **同构价差对比** | 同品种同跨度、前后腿注销属性相同、跨注销月数相同的价差互比，carry调整残差偏离/绝对价>**0.004** | \|偏离\| | 卖贵腿买贱腿（每组最多3条） |

### C 类（依赖历史日K/仓单，7 个，含 1 stub）

| 策略 | 触发条件 | score | 方向 |
|---|---|---|---|
| **季节性正套** | 同期分位<**20%** 且 最优夏普≥**2.0** | \|夏普\| | 做多价差(季节性低位)，返回建议持有期 |
| **季节性反套** | 同期分位>**80%** 且 最优夏普≤**−2.0** | \|夏普\| | 做空价差(季节性高位) |
| **价差统计套利** | \|全期分位−0.5\|>**0.45**(即分位<5%或>95%) 且 距到期>**30**天 且 样本≥200 且 **顺季节性**(同期季节漂移方向与交易方向一致,季节性夏普同号) 且 **期限结构历史同期过滤**(斜率分位>**90%**=深度contango→正套/做多是接结构性飞刀,放弃；分位<**10%**=深度back→反套/做空同理放弃；PARAMS键 slope_pctl_hi/lo) | 偏离度 | 高位做空/低位做多 |
| **蝶式价差统计套利** | \|蝶式全期分位−0.5\|>**0.45** 且 \|前段\|/绝对价<**0.02** 且 样本≥200 | 偏离度 | 高位做空蝶/低位做多蝶 |
| **波动传递** | (前段20日波动−后段20日波动)/绝对价>**0.0015** | 归一差 | 前段波动显著高于后段(观察) |
| **注销月近月崩塌** | 价差=非注销前月−注销月；分三情形（仓单高压临近未定价→做多；仓单低折价过度→做空；远期已深度定价→做空） | 分位差/100 | 见条件 |
| **流动性溢价** | 主力距到期≤**75**天(换月窗口)；前月−主力被压低→做多，主力−后月被抬高→做空 | 换月进度(0→1) | 见条件 |
| ~~近月回归~~ | **未实现（桩）** | — | — |

**季节性/统计类共同前置**（`_hist_ctx`）：双边盘口 + 两腿 OI≥5000 + (相邻月对 或 1-5/5-9/9-1 循环对) + 历史≥4年，才做统计，控制成本。

## 五、全局过滤与标注（`_annotate` + `build_universe`）

- **F 结尾结算合约全系统排除**（`is_settle_f`）。
- **`LIQ_MIN_OI=5000`**：任一腿 OI 不达标 → `liquid=False` → 钓鱼桶。
- **`ADVISORY_ONLY = {EC, T, TF, TL, TS}`**：集运/国债现金交割 → 仅提示。**股指(IF/IC/IH/IM)已移出**——可交易、可转抛(移仓吃基差)，正常进主列表。
- **`DIVIDEND_PRODUCTS = {IF, IC, IH, IM}`（股指分红逻辑）**：成分股分红集中 **3-9月**（年报分红），远月合约提前贴现分红 → 近−远价差在该窗口**偏大属季节性正常，不是错价**。命中且价差跨 3-9 月的机会，`conditions` 里打 `分红季` 标注。
  - 季节性/统计类走「同期分位」（同日历期对比）→ **已正确处理分红季**。
  - 纯资金 carry 口径（转抛 / 远期高低估）的 fullcarry **未扣分红**，该窗口比例会被高估 → 靠 `分红季` 标注提示，暂不自动扣减（如需精确，接入分红点数据做分红调整 fullcarry）。
- **`REGIME_BREAKS = {JM:[(202701,交割标准提高)]}`**：跨断层的结构偏移非错价 → 打标注排除。
- **`rollable`（可转抛）**：spread 类 + 正套方向(做多/买近卖远) + `can_roll`(区间[近,远)无注销月) + 近月未进交割月。股指无注销月且交割月豁免 → 自动 `rollable=True`。
- **`MAIN_CYCLE_159`**（M/Y/A/C/CS/L/PP/V/EG/RM/OI/CF/SR/TA/MA/FG/SA）：窗口外的 1/5/9 月合约也纳入，用于主力循环价差对比。

## 六、关键计算口径

- **远期/远月定义（用户口径）**：前腿（近腿）到期 **6 个月（180 天）以上**。远期高低估只看前腿>180天的价差；远月转抛的"远月对"=前腿>180天，其余为近月对。

- **fullcarry**（满仓持有成本）= 1.1×近月价×利率%/100×月数/12 + 仓储费×30×月数 + 时间贴水(decay)。展示为负。转抛比例 = mid/(−fullcarry)。
- **同期分位**：各历史年版在"今天同期日历位置"取值 → 当前值在其中的百分位。
- **全期分位**：当前值在该段全部历史样本中的百分位。
- **最优夏普**（注释39口径）：各年自同期位置起的未来日收益跨年取均值序列，夏普(d)=前d日均值之和/其标准差，遍历 d=5..60 取 \|夏普\| 最大。正=做多价差有利。
- **期限斜率**：`hist_stats._wls_slope` 持仓量加权回归(close~距到期月数)，元/月；季节性策略附带当前斜率+斜率同期分位。
- **品种期限结构判定 `curve_structure`（每条机会都带，报告有专列）**：用**同样特性的月份互比**——按是否注销月分组（如 FG/SA 奇数月=注销月），组内各自 OI 加权回归 close~距到期月数：
  - 各组斜率**同号且幅度相近(≤3倍)** → 判定整体 **back/contango**；同号但幅度差大 → 结构成立标"组差大"；**异号 → 分歧**（驼峰/规则性错位，整体斜率不可信）。
  - 整体斜率 = 组内去均值后的合并回归（固定效应，剔除注销/非注销组的水平差，元/月）。
  - 同时给 **斜率同期分位%**（当前斜率在历史各年同日历位置的分位）。
  - 报告新增两列：`期限结构`（如 back(-12.3元/月) / contango(+8.5元/月) / 分歧）、`斜率分位%`；CSV 同步 `curve`/`slope_pctl` 列。

## 七、可调参数与二次过滤的钩子

**阈值集中在 `screener.py` 的 `PARAMS` 字典**，通过 `_p(策略, 品种, key, 默认)` 读取，支持逐品种覆盖：`PARAMS[策略][品种]` 优先于 `PARAMS[策略]['*']`。目前 PARAMS 只显式配了折角类，**其余策略用的是各函数里的默认值**（上表加粗数字），要收紧就往 PARAMS 里加条目，无需改函数。

例：把流动性溢价限制得更严、且只保留价差同期分位极端的——
```python
PARAMS["流动性溢价"] = {"*": {"roll_days": 60}}   # 换月窗口 75→60 天
# 或在 s_liq_premium 里加：spp 不为 None 且 (方向做多要求 spp<30 / 做空要求 spp>70) 才输出
```

**当前值得优先收紧的点**（按最近一版结果）：

1. **流动性溢价 ~100 条**（占主列表最多）：现在只要主力进入换月窗口就对前后月各出一条，未强制价差分位。建议把 `价差同期分位%` 纳入硬条件（做多要求分位低、做空要求分位高），预计可砍掉一多半。
2. **同构价差对比 ~69 条**：每组最多 3 条已去重，但组很多。可提高 `N_norm`(0.004→更大) 或限制只保留每品种 top1。
3. **价差统计套利 ~56 条**：偏离阈值 0.45（分位<5/>95）已较严，但样本门槛 200 之外无流动性二次约束，可加距到期或 OI 加权。
4. **二次过滤最省事的做法**：不改策略，直接在 `run_screen` 后按 `conditions` 里的字段做后置 filter（比如"只要 score>X 或 同期分位<10 或 组合合约非空"），或在页面筛选栏加对应控件——因为所有判据都已存在 `conditions`/CSV 里。

**后置过滤可用字段一览**（存在于 `opportunities.csv` 的 `conditions` 列）：同期分位%、全期分位%、最优夏普、持有期、期限斜率、斜率同期分位%、仓单同期分位%、当前仓单、转抛比例、z、每月carry率、换月进度、最小腿OI、距到期 等。

---

*想怎么过滤告诉我口径即可：改 PARAMS 阈值 / 加硬条件 / run_screen 后置 filter / 页面加筛选控件，四种都行。*
