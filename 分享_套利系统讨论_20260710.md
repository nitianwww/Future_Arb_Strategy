# 期货跨期套利系统 — 近期进展与功能讨论（2026-07-10）

## 项目是什么

基于天勤 TqSdk 的国内期货**跨期套利**系统，覆盖上期所/能源中心/大商所/郑商所/广期所/中金所全部 88 个品种。整体分四部分：

1. **历史数据**：日K缓存（4700+ 合约，含持仓量）、仓单数据（RQData）、每日 tick 落盘、机会快照增量归档
2. **机会筛选与提醒**：14 个策略自动扫全市场，输出静态 HTML 报告
3. **下单算法**：交易所组合合约（DCE.SP / CZCE.SPD / GFEX.SP）被动到价单，dry-run 已验证，实盘待测
4. **管理界面**：纯静态 HTML + ECharts，无需服务器

## 筛选策略（14 个）

**实时类（A 类）**：折角修复、主力合约折角修复、注销月折价、价差递延、注销后首月溢价、远期高低估、近月转抛、远月转抛、同构价差对比

**统计类（C 类，基于历史同期分位）**：季节性正套/反套、价差统计套利、蝶式价差统计套利、波动传递、注销月近月崩塌（结合仓单）、流动性溢价（主力换月）

输出分三段：**主列表**（流动性达标）、**钓鱼桶**（最小腿持仓 < 5000 手，只挂单钓鱼不追）、**仅提示**（股指/国债/集运等只提醒不建仓）。

每条机会自动标注**可转抛**（只能正套方向 + 近远月之间无仓单注销月 + 未进交割月），并链接到品种详情图（7 面板：价格结构 / 相邻价差与成本线 / 月差走势墙 / 主连季节性 / 主力持仓季节性 / 仓单季节性 / 期限结构斜率季节性）和历史同期统计页。

## 本次新增：期限结构斜率

给每个品种按**持仓量加权回归**（收盘价 ~ 距到期月数）算期限结构斜率，进入：

- 季节性策略的触发条件（当前斜率 + 斜率同期分位）
- 历史同期页图 2、品种详情页第 7 面板

验证案例：豆粕 M 斜率 -11.75 元/月、同期分位 100%（历史同期最陡 back），与 9-1 价差处于季节性低位互相印证，m2609-m2701 正套跳到全市场第 2（score 21.5）。

## 本次新增：选中交易机会（重点讨论）

需求：在机会列表页人工"选中"感兴趣的机会，集中管理，且**每天列表刷新后已选中的记录不能变**。

设计要点：

- 每行一个「选中」按钮，页面顶部一个 📌 已选中区，带「移除」按钮
- **存储在浏览器 localStorage，与页面文件完全解耦**——报告每天重新生成，选中区不受影响
- 关键决定：存的是**点击瞬间的整行快照**（当时的分数、触发条件、时间戳），不是引用。之后行情变了、机会从列表消失了，选中区看到的还是"选中那一刻"的样子——"历史不会变"是结构性保证，不依赖数据配合
- 按"策略 + 结构 + 方向"去重，已选中的行按钮自动变灰

后又追加「**＋自定义新增**」：手工录入品种/结构/方向/备注，加进同一个列表（绿色底区分），用于记录筛选器没覆盖的想法。

已知局限：localStorage 绑定本机浏览器，清浏览器数据会丢。后续如果要喂给下单程序，会升级成文件存储。

## 最新一轮全量筛选结果（2026-07-10 13:03）

总 638 条 | 主列表 322 | 钓鱼桶 293 | 仅提示 316。

主列表 TOP：豆油 Y 注销月折价蝶式（y2609-2611-2612，score 21.5）居首；豆粕 M 9-1、棕榈 P 12-1、玻璃 FG 6-1、棉花 CF 11-1、硅铁 SF 12-1、锌 ZN 10-11 等季节性低位正套跟随，其中多条带可转抛标记。

## 顺带的工程心得（用 Claude Code 长期做项目）

这次对话把上下文用爆了，处理方式值得记一笔：

- **持久知识不放对话里**：品种规则做成 skill 文档、项目进度写进记忆文件、大输出重定向到日志/CSV——对话本身随时可丢
- **一个任务一个会话**：跨太多阶段的长对话会被自动压缩，多压几轮细节流失，不如主动开新会话
- 阶段结束说一句"更新记忆"，下个会话无缝接上

---

*系统全部为静态 HTML + Python 脚本，无服务器依赖；数据源：TqSdk 专业版（行情/K线/tick）+ RQData（仓单/主力序列）+ 各交易所官网（规则核对）。*
